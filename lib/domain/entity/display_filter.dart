/// カード一覧・マップに横断して効く「取得状態」による絞り込み。
///
/// マップ座標種別のような画面固有の表示オプションとは異なり、これは一覧・マップ
/// のどちらにも同じ意味で適用される横断的な絞り込み条件。
enum DisplayFilter {
  /// 絞り込まない（すべて表示）。
  all,

  /// 取得済みのみ表示。
  acquired,

  /// 未取得のみ表示。
  unacquired,
}
